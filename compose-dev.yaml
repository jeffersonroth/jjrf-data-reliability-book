services:
  app:
    entrypoint:
      - sleep
      - infinity
    image: jeffroth/mdbook-environment-dev:latest
    init: true
    volumes:
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
